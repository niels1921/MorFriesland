@model IEnumerable<MorFriesland.Models.Melding>

@{
    ViewData["Title"] = "Index";
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Beschrijving)
            </th>
            <th>
                <a asp-action="mijnmeldingen" asp-route-sortOrder="@ViewData["DateSortParm"]">@Html.DisplayNameFor(model => model.Opgelosttijd)</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beschrijving)
            </td>
            @if (item.Opgelosttijd != null)
        {
            <td>
                @Html.DisplayFor(modelItem => item.Opgelosttijd)
            </td>

    }
    else
    {

        <td>
            nog niet opgelost

        </td>
}
        </tr>

}
    </tbody>
        </table>
